import {newRidgeState} from 'react-ridge-state';

export const userData = newRidgeState({});
export const deviceId = newRidgeState();
export const deviceIdData = newRidgeState();
export const languageString = newRidgeState();
export const endpoints = newRidgeState(null);
export const colorData = newRidgeState(null);
export const gLoading = newRidgeState(false);
